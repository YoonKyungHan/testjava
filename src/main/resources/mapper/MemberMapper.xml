<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    파라미터는 상황에 맞게
    - 단순값/여러키: hashmap
    - 화면 VO 그대로: VO
-->
<mapper namespace="com.example.mapper.MemberMapper">

    <!-- 올해 최대 po 조회 (HashMap) -->
    <select id="selectMaxPo" parameterType="hashmap" resultType="string">
        SELECT MAX(PO)
          FROM MEMBER
         WHERE PO LIKE CONCAT(#{prefix}, '%')
    </select>

    <!-- 회원 단건 조회 -->
    <select id="selectMember" parameterType="string" resultType="com.example.vo.MemberVO">
        SELECT PO
             , MEMBER_ID
             , MEMBER_NAME
             , EMAIL
             , PHONE
             , AGE
             , GENDER
             , ADDR
             , DEPT_CD
             , USE_YN
             , JOIN_DT
          FROM MEMBER
         WHERE PO = #{value}
    </select>

    <!-- 회원 등록 (VO) -->
    <insert id="insertMember" parameterType="com.example.vo.MemberVO">
        INSERT INTO MEMBER (
              PO
            , MEMBER_ID
            , MEMBER_NAME
            , EMAIL
            , PHONE
            , AGE
            , GENDER
            , ADDR
            , DEPT_CD
            , USE_YN
            , JOIN_DT
        ) VALUES (
              #{po}
            , #{member_id}
            , #{member_name}
            , #{email}
            , #{phone}
            , #{age}
            , #{gender}
            , #{addr}
            , #{dept_cd}
            , #{use_yn}
            , #{join_dt}
        )
    </insert>

    <!-- 회원 수정 (VO) -->
    <update id="updateMember" parameterType="com.example.vo.MemberVO">
        UPDATE MEMBER
           SET MEMBER_ID   = #{member_id}
             , MEMBER_NAME = #{member_name}
             , EMAIL       = #{email}
             , PHONE       = #{phone}
             , AGE         = #{age}
             , GENDER      = #{gender}
             , ADDR        = #{addr}
             , DEPT_CD     = #{dept_cd}
             , USE_YN      = #{use_yn}
             , JOIN_DT     = #{join_dt}
         WHERE PO          = #{po}
    </update>

</mapper>
